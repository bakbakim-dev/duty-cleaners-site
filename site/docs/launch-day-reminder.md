# Launch-day reminder — owner request, September 13, 2026

When the owner announces launch or readiness to go live in this conversation, bring up this checklist and recheck the latest code and deployment evidence. A daily thread heartbeat is also registered as `duty-cleaners-launch-checklist`; it stays quiet until that announcement. Do not mark an item complete merely because its source code exists.

SiteGround is the chosen production host. `mikaily131.sg-host.com` is the staging host. Earlier instructions to buy Netlify production capacity are superseded by the owner's SiteGround decision.

## Before switching production traffic

- Back up the existing WordPress site and database, record current DNS and email records, and prepare a rollback. Confirm the actual deployment target and HTTPS certificate. Preserve working MX, SPF, DKIM and DMARC records during the change.
- Completed 2026-09-20: the production sitemap crawl checked all 210 canonical URLs; all returned 200, with no missing canonical, accidental noindex or duplicate title. HTTP/www/slash and preserved Red Deer redirects, a real 404, production canonicals, JSON-LD, HSTS and the intended cache headers were verified live. The generated Apache rules and PHP endpoints are present on SiteGround.
- Turn OFF NGINX Direct Delivery on the production SiteGround site (Site Tools → Speed → Caching → NGINX Direct Delivery). It is per site, so a new site for the real domain starts with it ON. While ON, nginx serves static files before Apache reads `.htaccess` and imposes its own TTLs — 180 days on `robots.txt`, `sitemap*.xml`, `llms.txt` and any `index.html` fetched by file name, one year on `logo.png`, `og-image.jpg` and `favicon.ico` — which defeats the content-dates ledger's sitemap refresh and the hourly revalidation of stable-named images. Disabled on `mikaily131.sg-host.com` 2026-09-17; verified live that every type then matches the generated rules (HTML `max-age=0, must-revalidate`, sitemaps/robots 1 h, images 1 h, `/assets/` one year immutable). Dynamic Cache and the CDN honour these headers, so they may stay on. Confirmed OFF on production 2026-09-20: every type matches the generated rules live (HTML `max-age=0, must-revalidate`; `sitemap.xml`, `llms.txt` and `robots.txt` 1 h; `logo.png` and `favicon.ico` 1 h `must-revalidate`; `/assets/` one year `immutable`).
- HSTS ramp: the site ships `Strict-Transport-Security: max-age=86400` on the production host only. After one clean week on the domain (no certificate or redirect incident), raise it to `max-age=63072000` in `scripts/generate-htaccess.ts` and `public/_headers`, update the proof in `scripts/guard-proofs.ts`, regenerate. Add `includeSubDomains` only once mail., ftp. and autodiscover. are confirmed to serve HTTPS or nothing at all; do not submit to the preload list. Production served `max-age=86400` when checked 2026-09-20, so the earliest clean-week raise is 2026-09-27.
- Re-read both Google listings (CIDs in `google-listings.ts`) on launch day and update proof.ts, `public/llms.txt` and `public/llms-full.txt` together; policy.test.ts fails once a read is 90 days old.
- SiteGround appends `Crawl-delay: 10` to robots.txt responses server-side (their KB: default for all agents, override by stating your own value, minimum 1). It is inserted into the LAST group of the file, which in ours is `User-agent: *`; Googlebot and Bingbot each have their own group at the top and read only that, so they are not throttled. Seen on the test host 2026-09-17. Superseded 2026-09-19 (5a9752c era): robots.txt now states `Crawl-delay: 1` in the `*` group itself, and production serves that value, not SiteGround's 10 (verified 2026-09-20). Bing obeys a catch-all delay even where it has its own group, which is why the line is stated rather than left to the host.
- Completed 2026-09-20: the SiteGround-native encrypted lead queue and five-minute retry cron are live. A controlled browser quote received a durable receipt in under a second and later reached GHL. Preserve the clock-skew guard in `ghl-quote.php`; SiteGround's server clock was about three minutes behind the browser at launch.
- Completed 2026-09-20: the GHL Instant Quote Automation is published. Production execution logs show the admin email, customer SMS, New Lead-Forms opportunity and trigger-tag removal all executed, and the workflow finished. Contact and cleaner-application content paths still need their own periodic live tests.
- Completed 2026-09-20: the GHL After-Hours Quote Reply workflow is published. Its trigger is restricted to SMS replies to Instant Quote Automation contacts with `quote-confirmed`; the office-open branch uses five OR segments covering Edmonton/Calgary Monday-Saturday 8:00 a.m.-8:00 p.m. and Sunday 9:00 a.m.-3:00 p.m., plus Red Deer Monday-Saturday 7:00 a.m.-9:00 p.m. The closed branch sends the owner-approved booking-confirmation warning from +15878124907. Re-entry is off to prevent repeated automatic replies. No live test SMS was sent during setup.
- BookingKoala's published receiver was changed to `https://dutycleaners.ca/api/booking-handoff.php` on 2026-09-20. Production seal, unseal, field round-trip, tamper rejection and foreign-origin rejection passed; the public script contains the production endpoint and no Supabase handoff reference. The production browser journey retained service/property choices, name, address, custom answers and notes. Owner-approved valid contact details changed BookingKoala's native email and phone controls from invalid to valid; the exact existing email also triggered BookingKoala's native returning-customer password state. BookingKoala redraws those input properties blank even through its own documented query path, so the visual blank is not evidence of a lost handoff. Still run expiry/history checks and a controlled final booking to prove saved-record persistence.
- Test the funnel, contact form and cleaner application on mobile and desktop. Check successful submission, timeout, retry, saved contact, displayed price, extras, complete service address, cleanliness, parking, entry instructions and flexibility. Keep name/email/phone before price, preserve answers on failure, and do not conceal the calculated price if lead capture fails.
- Test BookingKoala availability, transferred field readback, final total/GST and booking confirmation using an approved controlled booking. The website handoff itself is not a completed booking.
- Form-health production endpoint and private sender/recipient configuration were installed on 2026-09-20. A controlled production failure, immediate duplicate and recovery proved that the first failure generated one inbox alert, the duplicate generated no second email, and recovery generated one inbox alert at `info@dutycleaners.ca`. SPF, DKIM and DMARC passed. The GHL relay is live and a controlled production quote completed its workflow. Independent uptime monitoring, periodic retests and routine log-rotation checks remain.
- Completed 2026-09-20: independent UptimeRobot monitoring checks every five minutes and sends immediate email alerts to the Google Workspace `info@dutycleaners.ca` mailbox. Four monitors cover homepage HTTP, homepage `Duty Cleaners` content, the form-health response's `"alerts_enabled":true`, and BookingKoala's `Duty Cleaners` content. All four completed their first external check as Up with 100% uptime and zero incidents.

## At the domain switch

- Confirm production has no staging password/noindex/X-Robots-Tag or accidental robots block. Keep staging and obsolete previews protected from indexing. Check rendered page content, production canonicals, sitemaps and JSON-LD on the actual host.
- Found and fixed 2026-09-20: `mikaily131.sg-host.com` was a full, crawlable copy of the live site. Its noindex wall came down at the owner's request on 2026-09-18, when staging was still the only public copy, and the staged build rewrites canonicals to the staging host, so its pages self-canonicalised against production. The `X-Robots-Tag: noindex, nofollow` line was reactivated in staging's `.htaccess` (owner approved; edited in Site Tools' editor because an unrelated `public_html.zip` was sitting in the account root). Verified after: staging returns the header on the homepage, a money page, a location page and robots.txt while still serving pages; production returns no such header. Keep the wall up while both hosts serve the same build, and re-check it after any staging upload — a normal upload excludes `.htaccess`, but a full-account restore would not. The three older previews were checked the same day: all reachable, all noindexed (`duty-cleaners-preview.netlify.app` and `dutycleaners-preview.netlify.app` by header, `bakbakim-dev.github.io/dutycleaners-preview` by meta).
- Reported done 2026-09-20 (owner, work carried out in another session): the GoHighLevel Private Integration token that was exposed in a screenshot has been rotated and the new token installed in the production relay. It is listed here because it had only ever been CLAUDE.md to-do 2. Not independently verified in this session; a live quote reaching GHL is the proof.
- GA4 launch setup checked 2026-09-20: production uses property `Duty Cleaners - GA4`, web stream `3636867999`, Measurement ID `G-5WNJ12G60D`; Realtime visibly received production page views. Enhanced Measurement keeps outbound clicks, form interactions and history-based page changes off because the site sends its own privacy-filtered events; email and the nine BookingKoala prefill query keys are redacted; Google Signals and user-provided data collection remain off. Event and user-data retention are both 14 months. `generate_lead` and `contact_enquiry_submitted` are configured as key events, counted once per event with no invented monetary value. Realtime proved a real `generate_lead` key event plus `quote_step`, `quote_price_view` and `contact_submitted` funnel events. A live contact enquiry returned `Message sent. It is with the office now.`, but the Chrome test profile remained opted out of analytics even after a third-party-cookie exception was added; one contact submission from a trackable browser is still required to prove `contact_enquiry_submitted` end to end. Treat `booking_handoff_succeeded` as an intermediate event, not a completed booking; use a verified BookingKoala confirmation/payment event for the final booked outcome when that integration exists.
- Completed 2026-09-20: the Search Console Domain property was verified in the owner account and `https://dutycleaners.ca/sitemap.xml` was submitted successfully. Google showed it as a successful sitemap index; the initial discovered-page count was zero while processing begins. The older WordPress sitemap entries remain in Search Console and can be retired after the replacement has been read. Key money pages passed the live 210-URL crawl. Bing Webmaster Tools remains outstanding.
- Test phone/email links and every form again on the real domain. Verify incoming/outgoing business email after DNS changes. Inspect PHP/server errors and monitor alerts.

## Outstanding integrations and follow-ups

- Completed 2026-09-20: `bun run content-dates` reported 0/210 changed content fingerprints after the production launch check; no revision dates needed stamping. Continue to review real later content changes rather than stamping every URL.
- Confirm Red Deer gift-card redemption eligibility before adding it to gift-card claims. Recover the four unknown legacy article publication dates from WordPress if possible; until then, keep publication dates omitted and use labelled revision dates.
- Supply consent-cleared team/subcontractor profiles, real job photos and documented case examples. An owner's portrait is not required. Do not substitute invented jobs, certifications or generated people presented as staff.
- Use fresh query-by-page Search Console data, booking evidence and backlinks before approving any location-page consolidation (including Parkhill/Stanley Park and the Diamond Valley historical areas). Preserve the existing URLs until a specific retirement and redirect is approved.
- Search Console on 2026-09-20 showed 38 mobile URLs needing improvement for CLS greater than 0.1, sourced from the previous 90 days and therefore primarily the replaced site; the report was last updated 2026-09-18. HTTPS showed 0 non-HTTPS URLs and no critical issue. Wait for enough post-launch field data before attributing the CLS group to the replacement, and repeat mobile lab/field checks meanwhile. The unauthenticated PageSpeed API was quota-limited (HTTP 429) during the launch check.
- Booking domain, still open (recorded only in the historical REBUILD-PLAN until 2026-09-20; `book.dutycleaners.ca` did not resolve when checked that day). Customers currently leave `dutycleaners.ca` for `dutycleaners.bookingkoala.com` at checkout. To finish it: (1) ask BookingKoala to enable the custom domain and issue its certificate, and take their CNAME target — a vendor-side settings change, so it needs the owner's go-ahead; (2) add the CNAME and wait until `https://book.dutycleaners.ca/booknow` serves their form; (3) in one commit, change `BOOKING_ORIGIN` (`src/lib/booking-redirect.ts`), `GIFT_CARD_ORIGIN` (`src/pages/GiftCard.tsx`, `GiftCards.tsx`), the `frame-src` and `form-action` hosts in `scripts/generate-htaccess.ts` and `public/_headers`, and the tests and proofs naming the old host; (4) retest a full booking journey, because the frame source, the handoff origin check and the prefill receiver all key off that host. `public/api/booking-handoff.php` already accepts both origins, so the encrypted handoff survives the switch. Same-site is also why it is worth doing: Chrome skips the cross-site preload for visitors who already hold BookingKoala cookies.
- Reconcile BookingKoala booking-created/updated/cancelled/completed/charged/declined events with GHL; deduplicate by booking ID and stop quote follow-up after booking. Confirm the exact supported triggers before setup. Avoid duplicate customer reminders or counting automatic recurring bookings as new acquisitions.
- Correct Edmonton Yelp address/duplicate issues and claim Calgary before reconsidering those links. Review Red Deer's business category and start requesting genuine customer reviews through the business's normal process.
- Replace generated people images with permission-cleared photographs of the actual team and jobs; retain the existing real-photo brief.
- Identify the optional alternative products if the owner wants them named. Keep current factual wording until confirmed.
- Retire obsolete public previews after confirming the replacement; do not delete the rollback backup.
- Verified on production 2026-09-20: all 259 URLs the old WordPress site linked or listed were probed against `dutycleaners.ca` (`scratchpad/migprobe.py`, the inventory from AuditSpur scan 1248 plus the old sitemaps). 258 reach a 200 in one permanent hop; six go to the homepage deliberately (Edmonton-themed pages). The single exception is `/services` without a trailing slash, which SiteGround itself answers 403 before `.htaccess` runs — the old site did the same, nothing links it, and `/services/` is canonical. Only SiteGround support can lift that rule.
- Review crawl/indexing errors, traffic and conversion changes after launch against the saved Search Console baseline.
- Incident 2026-09-21 (found and fixed): 209 of the 210 live pages referenced the 15:15 "live-schedule-only" build (`index-Cs5bttTf.js`), but later uploads had replaced `public_html/assets/`, leaving 193 of that build's page chunks and 17 map chunks missing. The HTML served 200, so crawlers and HTML-only checks saw working pages; real browsers hydrated, failed to fetch the page chunk and showed "We couldn't load this page" (the Calgary hub from Google froze the tab). Restored by adding exactly the 210 missing files from `duty-cleaners-live-schedule-only-20260920.zip` into `public_html/assets/` (no overwrites, byte-identical to the build). Verified: all 265 files the entry references answer 200, and all 210 pages hydrate to their own H1 with no error screen. Resolved 2026-09-21: one deploy from a single build (commit c7a1d0c, entry `index-BqNYNmZu.js`) — assets uploaded first in three parts, then the 212 HTML files. The homepage had also been left as a 5.6 KB unprerendered shell by the name-phone installer; it is prerendered again. Verified: all 923 files match the build byte for byte, all 694 files the pages reference answer 200, and all 210 sitemap pages hydrate on `index-BqNYNmZu.js` to their own H1 with no error screen. Rule stands: deploy HTML and assets from one build, assets first, and never delete older hashed files in `assets/` during a deploy. Follow-up the same night: revision date 2026-09-20 approved for `/` and `/cleaning-services-calgary/` (commit cf2fddc); rebuilt, and the changed files deployed (288 new assets, then 212 pages and `sitemap-main.xml`). All 210 pages verified hydrating on `index-JL8-D-Z2.js`. The server root was cleared of the day's release zips, staging folders and saved homepage copies; kept: `public_html-before-address-bias-fix-v2-20260920` (latest full backup) and the WordPress rollback archive (`legacy-wordpress-2026-09-20`, `dutycleaners-prelaunch-old-site-2026-09-20.zip.zip`). The three token-bearing installer scripts that were live in `public_html/` were deleted. A "200 on every sitemap URL" check does not prove pages work: also fetch every chunk the live entry references, or load the pages in a browser.

Reference implementation details: `form-health-monitoring.md`, `booking-handoff-implementation-2026-09-12.md`, `funnel-and-launch-implementation-2026-09-12.md`, and the repository-root `CLAUDE.md`. Recheck dated statements before repeating them as current facts.

## AI-generated images: IPTC disclosure (found 2026-09-17)

Google's generative-AI content guidance (developers.google.com/search/docs/fundamentals/using-gen-ai-content,
updated 2025-12-10) says AI-generated images must carry IPTC `DigitalSourceType` =
`trainedAlgorithmicMedia` metadata. Every people image in the library is AI-generated
(PHOTO-SHOOT-BRIEF.md). Two steps, owner's call on the list: (1) name which source files under
`site/src/assets/` are AI-generated; (2) stamp them once with sharp's `withXmp()` (installed with
vite-imagetools) and set `removeMetadata: false` in the imagetools config so the generated variants
keep it. Google may then label those images "AI-generated" in Image Search. Moot for any image the
photo shoot replaces.
- 2026-09-21 funnel and booking-page polish deployed (commits 113fab6, b87e390, 734d48a): Deep Cleaning card and naming, single price summary, phone-first price step, one Add control, nav from 1024px. Site: 288 assets then 212 pages, verified byte-identical, 694 referenced files 200, all 210 pages hydrate on `index-BKxyOz0A.js`. BookingKoala: header code now carries the brand block (bk-brand.css) ahead of the handoff script; logo replaced with brand/booking-logo.png; Book Now hero set to neutral text ('Book your clean'), which the receiver swaps for 'Here's your price — pick a date to lock it in' only when quote details arrive. Theme Builder note: text typed into its TinyMCE field updates the canvas but is only published after an extra keystroke in the editor (type a space, delete it, click outside); check the live page after publishing.
- 2026-09-21 funnel review deployed (commit a22789c): counting rule at the bedroom question, no price before contact details, equal first-clean and per-visit figures, unmissable missed-question flags with a jump-link summary, sans step headings, one booking label. Site: 288 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-CF2OXe2C.js`. PHP relays, robots.txt and .htaccess were not uploaded (unchanged). Both release zips deleted from the server root.
- 2026-09-21 funnel plan and lead fixes deployed (commit 8c69954): no preselected plan (the lead reaches GoHighLevel with the plan blank until the visitor picks one; the price card lists every plan's per-visit price; "How often?" is required), and one lead per quote (going back to change the home returns straight to the updated price). Site: 287 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-BZZhBikL.js`. Both release zips deleted.
- 2026-09-21 move-out card fix deployed (commit a521701): the funnel's Move In / Move Out card lists its checklist items instead of claiming it includes deep cleaning. Site: 287 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-Bt4LfzfN.js`; the live card shows the new text. Both release zips deleted.
- 2026-09-21 per-branch ratings deployed (commits 10ecd9b, db22c4e): the quote funnel, the quote overlay, every Calgary page and the branch-specific shared components read their own Google listing (CALGARY_RATING_CLAIM / EDMONTON_RATING_CLAIM / ratingClaimFor); RATING_CLAIM remains only on company-wide lines naming both offices, and a guard fails the day the two listings' ratings differ (then reword those lines per branch). Both read 4.9, so no page text or revision date changed. Site: 287 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-C-FZdWhg.js`. Both release zips deleted.
- 2026-09-21 booking-return fix deployed (commit d1410ec): the funnel saves its answers to this tab's sessionStorage at handoff (lib/quote-return.ts), and a Back/Forward load of the same page within two hours reopens it on the details step with everything filled in; the snapshot is removed once used, on any ordinary load, on a back/forward-cache restore and when the tab closes. Site: 287 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-DHoPjtMI.js`. Both release zips deleted. Still to check on a phone: Back from the real BookingKoala page.
- 2026-09-21 funnel Back-button fix deployed (commit 878575f): the browser's Back and Forward move between funnel steps (each step pushes a history entry carrying `quoteStack`); Back on the first step closes; once contact details are in, the price replaces the contact step in history; closing unwinds every funnel entry; Back during the handoff screen cancels the handoff. Site: 287 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-CyWQR4o4.js`; on the live site Back went contact step → home questions → closed. Both release zips deleted.
- 2026-09-21 funnel taste pass deployed (commit f4a0d8c): one-line sentence-case progress row, orange only on the primary button and progress fill, card sized to the step, two radii, tokenised notices, one focus colour, press feedback, sentence-case labels, reassurance beside the button, phone bar legible at 375px. Site: 288 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-C9WjpImo.js`; the live funnel opens on "Step 1 of 4 · Tell us about your home". Both release zips deleted.
- 2026-09-21 funnel hierarchy pass deployed (commit 0d832ec): only the chosen service card is filled, Standard carries a "Start here" cue, the home-size questions weigh the same as the service question, and step 3 keeps the price card as its one elevated surface with ruled sections below. Site: 288 assets then 212 pages from one build, verified byte-identical; all 711 referenced files 200; sampled pages hydrate on `index-zywUpTi4.js`; the live funnel shows the cue. Both release zips deleted. A brief home-router DNS failure for *.siteground.com interrupted the File Manager mid-session; it recovered on its own.
- 2026-09-21 booking-page taste pass published in BookingKoala (commit 0ea2e07): Theme Builder header code replaced with the regenerated bk-header-fill.html. Verified on the live booking page: navy tab underlines and summary labels, 16px fields, no #fc6011/#00d0ff left. The same publish fixed a live encoding fault: the previous paste had turned the receiver's ’ — … into mojibake, so funnel arrivals saw "Hereâ€™s your price â€” pick a date". Paste the snippet from `Get-Content -Raw -Encoding UTF8` into `Set-Clipboard`, then confirm no U+00E2 U+20AC sequences in the box before saving.
- 2026-09-21 funnel copy + call-back marker deployed (commits d231f57, d52747c), including `api/ghl-quote.php`: the server copy was first confirmed byte-identical to the previous repo version (copied out of the File Manager editor and diffed), then replaced. A call-back request now arrives with source "dutycleaners.ca instant quote (call-back requested)", a contact note, and the new `callback-requested` tag. Relay probe after deploy: invalid POST returns its own 400 JSON; form-health 200. Site: 287 assets + relay, then 212 pages; all byte-identical; 711 referenced files 200; sampled pages hydrate on `index-Cms57EpB.js`. Completed the same evening: GoHighLevel workflow "Call-back Request Alert" is published (crm.bookin60.com, workflow 30614991-e143-4220-9ea0-c23aeef73a86). Trigger: Contact Tag, Tag added = `callback-requested`. Actions: internal notification email to support@dutycleaners.ca (the address the Instant Quote Automation uses), subject "Call-back requested: {{contact.name}}", body with name, phone, email and a pointer to the contact record; then remove the `callback-requested` tag. Allow re-entry is on, so a later request alerts again. The Instant Quote Automation still runs alongside it. Not yet exercised by a real call-back request.
- 2026-09-22 funnel review pass and owner edits deployed (commits 6a23b7b, 782e985, 8c109e3): move-out empty-home note, the merged three-lens review, e-transfer by phone (paid in full the day before), the handoff text without the booking host or late fee, and "may call about your booking". Same session, in BookingKoala: Deep Cleaning row 152 (6 bedrooms, $199.99) set to "Apply only to the first appointment" and exempt from the plan discount, like rows 146-151 (reopened after saving to confirm). Site: 290 assets then 212 pages from one build, verified byte-identical (both sitemaps matched with a cache-buster; the plain URL served SiteGround's one-hour cached copy); all 711 referenced files 200; the live funnel hydrates on `index-CBxtXr88.js` and opens on "Step 1 of 4". Both release zips deleted.
- 2026-09-22 area question, travel fee in the funnel price and lighter last screen deployed (commit ea83f9b). Step 1 asks where the home is; "Near ..." adds BookingKoala's travel-fee row to the price and the handoff ticks it. Checked end to end on a local build: a 2-bed standard clean near Calgary showed $198.99 ($208.94 with GST) in the funnel, and BookingKoala opened with the fee ticked at the same $198.99 / $208.94. BookingKoala header code: the new third block "travel fee by postal code" published in Theme Builder (the live header was first confirmed identical to the previous committed snippet, and the pasted text was hash-checked against bk-header-fill.html). Live test on /booknow without submitting: T8N ticks the fee ($169.00 to $198.99), T5J and T4N untick it, each with its note. Site: 293 assets then 212 pages, verified byte-identical (three sitemaps matched with a cache-buster); all 711 referenced files 200; the live Leduc page hydrates on `index-CJLomQvt.js` and shows "Home in Leduc" with the fee. Both release zips deleted. Completed the same day: BookingKoala's own "Payment Information" section text (Theme Builder, Home Cleaning form 1) no longer says the hold "is not" a charge. It now reads "You won't be charged today. Your card is only charged after your clean is completed." (bold) then "The day before your appointment, a temporary hold for the price goes on your card. On a debit card, that amount is set aside until the charge." Verified on the live /booknow page, with the travel-fee header still running. The Post Construction, Airbnb and Office forms carry no hold text. Edit it by typing in the section's TinyMCE box: setting it through the editor API updates the preview but is not saved by Save & Publish.
- 2026-09-22 lighter area questions deployed (commit 915aa30). Step 1 asks "Where is the home?" (Edmonton / Calgary / Red Deer area) only on the homepage and branch-less pages; the price step asks "Is the home inside <city> city limits?" (required, beside pets), and "No, a nearby town" adds the travel fee to the price ($169 to $198.99 near Calgary, checked). The overlay header's phone and rating now follow the chosen office. Site: 289 assets then 212 pages, byte-identical; all 711 referenced files 200; the live homepage hydrates on `index-DRVCyCFS.js`, shows the three-button question, and a Calgary answer switches the header phone to Calgary's. Both release zips deleted.
- 2026-09-22 prerender own-render fix shipped (PR #2, merged as 77d5ed7): the only runtime change is the `data-load-error` marker on the page-load error screen, which the prerender uses to refuse a failed render. Built from e683ee1 (1776 tests pass, 210 pages each with their own canonical). Site: 287 assets then 212 pages, byte-identical; all 711 referenced files 200; the served bundle `index-DFzxjAO2.js` carries the marker, and the live Leduc page hydrates and opens the funnel. Both release zips deleted.
- 2026-09-22 price-step rewards and extra-work wording deployed (commits acde458, 4795edc, b7aa69c). Price step: plan cards with per-visit price and dollar saving, rolling totals, change notes, a progress bar to "All set", and the fix for the phone overflow that cut off the "Almost there" button. Copy: the price is set by home size for the condition described, extra work is agreed before it is done, work beyond the booked visit is scheduled separately; "not a clock / stays until done / does not change however long" removed from 172 pages (dates approved) and guarded. Site: 292 assets then 212 pages, byte-identical (four sitemaps matched with a cache-buster); all 711 referenced files 200; live pages carry the new answer and no "not a clock"; the live Calgary hub hydrates on `index-CwXWiXlN.js` and opens the funnel. Both release zips deleted. OPEN: the "Duty Cleaners Content Prompt" artifact could not be republished from this account; the markdown in the repo is current.
- 2026-09-22 step-1 location question removed (commit a1f019e). The homepage and branch-less pages now go straight to the home questions; their price step asks "Is the home inside Edmonton or Calgary city limits?" (Red Deer left out for now, owner) and their leads carry no city tag. City, service, town and Red Deer pages unchanged. Site: 288 assets then 212 pages, byte-identical (sitemap-main matched with a cache-buster); all 711 referenced files 200; the live homepage hydrates on `index-1_CK0rIW.js` and opens the funnel with no location question. Both release zips deleted.
- 2026-09-22 price-card trims deployed (commits 4840305, db22f96): the notes under the price are gone (travel-fee note except for post-construction, add-on repeat notes, size line, condition bullets, re-clean line), the reassurance row no longer repeats on the price screen, "Your clean" shows only once something is added, and the bookings line no longer sits under the price heading. The extra-work rule stays on the cleanliness question. Site: 288 assets then 212 pages, byte-identical (sitemap-main matched with a cache-buster); all 711 referenced files 200; the live Calgary hub hydrates on `index-nRqZ71we.js` and opens the funnel. Both release zips deleted.
